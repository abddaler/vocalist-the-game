/**
 * Платформенные константы (раздел 2 мастер-документа).
 * Внутреннее разрешение фиксировано; игра не знает реальный размер экрана.
 */
export const INTERNAL_WIDTH = 480;
export const INTERNAL_HEIGHT = 270;

/** Минимальная область тапа — 16x16 внутренних пикселей. */
export const MIN_TAP_SIZE = 16;

/** Ключ, под которым SaveAdapter хранит слот сохранения. */
export const SAVE_KEY = 'vocalist-sim/save/v1';
