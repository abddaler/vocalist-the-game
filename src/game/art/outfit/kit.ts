import type { Frame } from '../body';

/**
 * Одежда: собственный слой поверх голого тела, ряды 11..23. Символы:
 * 6 — ткань, 7 — её тень, 8 — вторая ткань (рубашка, подкладка, вставка),
 * 9 — низ (брюки, юбка), B и C — акцент и его тень.
 *
 * Раньше одежда была просто цветом торса, и все на улице отличались
 * только оттенком футболки. Настоящий силуэт работает сильнее любого
 * цвета: пиджак виден со спины, юбка — в профиль, а майка оставляет
 * открытыми плечи, и человек в ней сложён иначе, чем человек в пальто.
 *
 * Ключ к этому — длина рукава. Тело нарисовано с руками вдоль корпуса;
 * ряд во всю ширину закрывает их тканью, узкий ряд оставляет открытыми.
 */
export type OutfitStyle =
  | 'tee'
  | 'tank'
  | 'jacket'
  | 'hoodie'
  | 'suit'
  | 'dress'
  | 'crop'
  | 'track'
  | 'coat';

export interface OutfitFrames {
  readonly front: Frame;
  readonly back: Frame;
  readonly side: Frame;
}

/** Ряд, с которого начинается одежда: плечи. */
export const OUTFIT_TOP = 11;

/** Ряд во всю ширину: рукав закрывает и плечо, и руку. */
const SLEEVE = '16666666666661';
/** Плечевой ряд: на пиксель уже рукава, иначе фигура выходит буквой «Т». */
const YOKE = '.166666666661.';
/** Узкий ряд: ткань только на корпусе, руки остаются голыми. */
const CHEST = '....666666....';
/** Плечи под рукавом. Для майки и платья вместо него идёт CHEST. */
const SHOULDER = YOKE;
const HEM = '..1777777771..';

const TROUSERS: Frame = [
  '..1999119991..',
  '..1999119991..',
  '..1999119991..',
  '..1999119991..',
];

const SIDE_TROUSERS: Frame = [
  '....199991....',
  '....199991....',
  '....199991....',
  '....199991....',
];

const SIDE_SLEEVE = '..1666666661..';
const SIDE_CHEST = '....666666....';
const SIDE_HEM = '...17777771...';

/** Заготовки рядов доступны обоим наборам одежды. */
export const ROWS = {
  SLEEVE,
  YOKE,
  CHEST,
  SHOULDER,
  HEM,
  TROUSERS,
  SIDE_TROUSERS,
  SIDE_SLEEVE,
  SIDE_CHEST,
  SIDE_HEM,
} as const;
