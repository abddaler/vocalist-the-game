/**
 * Все коэффициенты симуляции. В системах магических чисел быть не должно
 * (раздел 12). Каждое число снабжено комментарием, на что влияет.
 *
 * Цифры выставлены «по здравому смыслу» и будут перекручены на вехе 3,
 * когда headless-симулятор покажет реальные кривые (раздел 10).
 */
export const BALANCE = {
  time: {
    /** Длина среза в днях (раздел 1). */
    sliceDays: 60,
    daysPerWeek: 7,
    daysPerMonth: 30,
  },

  energy: {
    max: 100,
    start: 100,
    /**
     * Сколько возвращает ночной сон. Было 70 — при дневных тратах ~40
     * энергия стояла на потолке и не была ресурсом вообще. С 55 полный
     * день (распевка + урок + смена = 60) уже не влезает без передышки.
     */
    sleepRestore: 55,
    /** Ниже этого порога всё делается вполсилы. */
    lowThreshold: 20,
    /** Множитель эффективности при энергии ниже порога. */
    lowEfficiency: 0.5,
    /** Цена бессонной ночи (раздел 4: «ночь без сна — штраф»). */
    noSleepPenalty: { energy: -20, mood: -12, vocalHealth: -6 },
  },

  vocal: {
    max: 100,
    start: 100,

    /**
     * load = baseLoad * (loadAtZeroSupport - breathSupport/100)
     *        * genreMultiplier * (1 - warmupBonus)
     * При опоре 100 множитель 0.8, при 0 — 1.8 (раздел 6).
     */
    loadAtZeroSupport: 1.8,
    /** Скидка к износу за распевку в том же дне. */
    warmupBonus: 0.35,

    /** Пороги здоровья связок, раздел 6. */
    tiers: { normal: 70, fatigue: 40, hoarse: 20 },
    /** Множитель к оценке выступления на каждом пороге. */
    tierScore: { normal: 1, fatigue: 0.9, hoarse: 0.7, critical: 0.5 },
    /** Шанс травмы за одно вокальное действие на каждом пороге. */
    tierInjuryChance: { normal: 0, fatigue: 0, hoarse: 0.15, critical: 0.45 },

    /** Длительность травмы, дней (раздел 6). */
    injuryDays: { min: 5, max: 12 },
    /** Лечение у фониатра сокращает оставшийся срок вдвое. */
    injuryHealDivisor: 2,

    /**
     * Восстановление связок. Раздел 6 задаёт «полный день молчания +25»:
     * это три бодрствующих слота по silenceSlot плюс fullSilenceDayBonus
     * за то, что игрок промолчал весь день целиком (3*6 + 7 = 25).
     */
    recovery: {
      /**
       * В документе стоит +8, но при базовом износе 6/10/18 это давало
       * отношение износа к восстановлению примерно 4:1 против игрока:
       * новичок мог позволить себе одно вокальное действие в сутки, и
       * любая попытка учиться кончалась травмой. С 20 бережный день
       * (распевка + урок + смена ≈ 22) выходит почти в ноль, а жадный
       * день на три полноценных выступления по-прежнему убивает связки.
       */
      sleep: 20,
      silenceSlot: 6,
      fullSilenceDayBonus: 7,
      doctor: 40,
      tea: 3,
    },
    /** Сколько бодрствующих слотов в сутках (день без ночного слота). */
    wakingSlots: 3,

    /** Экстрим-техники закрыты, пока опора ниже этого значения (5.1). */
    extremeUnlockSupport: 45,
  },

  skills: {
    max: 100,
    /**
     * Затухание роста у потолка: gain *= 1 - (current/max)^exponent.
     * Чем выше exponent, тем дольше держится быстрый рост в начале.
     */
    diminishingExponent: 1.6,
    /** Опора — множитель к эффективности всех остальных статов (5.1). */
    supportFloor: 0.6,
    supportSpan: 0.4,
    /** Влияние настроения на скорость роста. */
    moodFloor: 0.7,
    moodSpan: 0.3,
    /** Стартовые значения самоучки. */
    start: {
      breathSupport: 8,
      range: 12,
      registers: 5,
      timbre: 10,
      diction: 15,
      pitch: 20,
      stamina: 10,
      extreme: 0,
      stage: 5,
    },
  },

  money: {
    start: 8000,
    /** Ниже этого долга приходит коллектор (раздел 5.2). */
    debtLimit: -3000,
    /**
     * Стоимость жизни. Симулятор показал, что при 2800/15000/2500 в долг
     * уходили все четыре стратегии: месяц обязательных трат съедал почти
     * весь заработок с подработки, и на уроки с одеждой не оставалось.
     */
    weeklyFood: 2200,
    monthlyRent: 12000,
    monthlyBills: 2000,
  },

  mood: { max: 100, start: 70 },

  fans: {
    /** Доля фанатов, уходящая за месяц без единого выступления (раздел 4). */
    monthlyDecayIdle: 0.18,
    /** Доля, уходящая за месяц даже при активности. */
    monthlyDecayActive: 0.04,
    /** Доля фанбазы, теряемая при смене жанра (раздел 7). */
    genreSwitchLoss: 0.5,
  },

  reputation: { min: -100, max: 100, start: 0 },

  /** Выступления (9.1). */
  performance: {
    /** Сколько песен тянет кто угодно; сверх этого — за счёт выносливости. */
    baseSongs: 2,
    /**
     * Каждые столько выносливости добавляют одну песню к запасу.
     * При 25 клубный сет из четырёх песен резал оценку на четверть даже
     * у подготовленного вокалиста — до клуба не доходил никто.
     */
    staminaPerSong: 18,
    /** Множитель качества за каждую песню сверх запаса выносливости. */
    overloadPenalty: 0.86,
    /** Разброс из формулы 9.1: rng(0.9..1.1). */
    jitter: { min: 0.9, max: 1.1 },
    /** Настроение как множитель к оценке. */
    moodFloor: 0.75,
    moodSpan: 0.25,
    /**
     * Опора как множитель ко всей оценке (раздел 5.1: «база, множитель к
     * эффективности всех остальных вокальных статов»). Без этого прокачка
     * опоры не влияла на выступление вообще — в весах жанров её нет, —
     * и стратегия «сначала опора» проигрывала по построению.
     */
    supportFloor: 0.55,
    supportSpan: 0.6,
    /** Вклад одного очка жанрового соответствия наряда в множитель. */
    outfitFitStep: 0.06,
    outfitMin: 0.8,
    outfitMax: 1.2,
    /** Доля выплаты по исходу. */
    payoutByOutcome: { fail: 0.3, ok: 1, good: 1.35, triumph: 1.8 },
    /** Доля славы по исходу. */
    fameByOutcome: { fail: 0, ok: 1, good: 1.6, triumph: 2.5 },
    /**
     * Минимальная отдача площадки, которую игрок перерос. Не ноль:
     * старую сцену не должно быть противно играть, просто невыгодно.
     */
    fameFloor: 0.08,
    /**
     * Настроение за выступление. У «нормально» ноль намеренно: с плюсом
     * бесконечное количество проходных концертов держало настроение
     * на потолке и выключало систему.
     */
    moodByOutcome: { fail: -12, ok: 0, good: 5, triumph: 10 },
    reputationByOutcome: { fail: -4, ok: 0, good: 1, triumph: 3 },
  },

  /** Конкурирующий вокалист (9.3). */
  rival: {
    startFame: 40,
    /** Насколько его слава растёт за день сама по себе. */
    growthPerDay: 2.2,
    /**
     * Отрыв, при котором он перехватывает выступление почти наверняка.
     * Шанс перехвата = (rivalFame - fame) / interceptSpread.
     */
    interceptSpread: 260,
    maxInterceptChance: 0.4,
  },

  /** Движок событий (9.4). */
  events: {
    /** Минимум слотов между событиями, чтобы они не сыпались подряд. */
    minSlotsBetween: 3,
    /** Шанс случайного события в подходящем слоте. */
    chancePerSlot: 0.22,
  },

  /** Синглы со студии звукозаписи (раздел 8, локация 6). */
  singles: {
    /** Сколько фанатов приносит один сингл за месяц. */
    fansPerMonth: 22,
  },

  career: {
    /** Слава, после которой можно нанять менеджера (9.3). */
    managerFame: 300,
    /** Доля выручки, которую забирает менеджер. */
    managerCut: 0.2,
  },

  log: {
    /** Сколько последних записей держим в состоянии. */
    maxEntries: 400,
  },

  /**
   * Нормы обстановки локаций. По ним ходит `npm run audit:locations`.
   *
   * Без чисел ревью локаций превращается в спор о вкусах: «пустовато» и
   * «слишком тесно» невозможно ни подтвердить, ни оспорить. Всё, что
   * считается машиной, машиной и должно считаться — глаза дороги, и
   * тратить их на подсчёт одинаковых стульев нельзя.
   */
  scenery: {
    /**
     * Доля пола под предметами. Ниже нижней границы сцена читается
     * недоделанной, выше верхней игрок не может пройти и не понимает,
     * куда смотреть.
     */
    fill: {
      street: { min: 0.3, max: 0.45 },
      apartment: { min: 0.45, max: 0.6 },
      restaurant: { min: 0.5, max: 0.65 },
      /** Танцпол пуст намеренно, поэтому нижняя граница ниже. */
      club: { min: 0.35, max: 0.5 },
      rehearsal: { min: 0.55, max: 0.7 },
      studio: { min: 0.6, max: 0.75 },
      shop: { min: 0.5, max: 0.65 },
      gym: { min: 0.55, max: 0.7 },
      clinic: { min: 0.4, max: 0.55 },
    },
    /** Сколько раз один и тот же предмет без вариации терпим в кадре. */
    sameKindLimit: 3,
    /** Со скольких предметов ровный ряд с равным шагом считается рядом. */
    rowLength: 3,
  },
} as const;
